/// Standard Uganda class level labels, by school stage.
class UgandaClassLevels {
  UgandaClassLevels._();

  static const primary = <String>['P.1', 'P.2', 'P.3', 'P.4', 'P.5', 'P.6', 'P.7'];

  /// O-Level. A-Level (S.5–S.6) isn't included here since it runs on
  /// subject combinations rather than a shared subject list — add those
  /// classes manually if the school offers A-Level.
  static const secondary = <String>['S.1', 'S.2', 'S.3', 'S.4'];
}